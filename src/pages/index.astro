---
import Layout from "../layouts/Layout.astro";
import posts from "../utils/getPosts";

const oracoes = posts
  .filter(post => post.frontmatter.categoria === "oração")
  .slice(0, 3);
---

<Layout>
  <main>
    <p class="boas-vindas">Seja bem-vinde ao nosso espaço espiritual 🌿</p>

    <section class="ultimos-posts">
      <h2>🕊️ Últimas Orações</h2>
      <div class="posts-list">
        {oracoes.map(post => (
          <article class="post-card">
            <img src={post.frontmatter.image} alt={post.frontmatter.title} />
            <h3><a href={post.url}>{post.frontmatter.title}</a></h3>
            <p>{post.frontmatter.description}</p>
          </article>
        ))}
      </div>
    </section>
  </main>
</Layout>
