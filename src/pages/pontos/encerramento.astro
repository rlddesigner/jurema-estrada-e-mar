---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const todosPontos = await getCollection('pontos');
const pontosFiltrados = todosPontos.filter(
  (p) => p.data.classe === 'ponto de encerramento'
);
---

<Layout title="Pontos de Encerramento" description="Cantos de fechamento e gratidão após o término dos trabalhos espirituais.">
  <main class="pagina-ponto">
    <h1>🔚 Pontos de Encerramento</h1>
    <p class="descricao-classe">Pontos para agradecer, fechar e harmonizar o ambiente após a realização das atividades espirituais.</p>

    <div class="grade-pontos">
      {pontosFiltrados.map(({ data }) => (
        <a href={`/pontos/${data.slug}`} class="card-ponto">
          <h3>{data.title}</h3>
        </a>
      ))}
    </div>
  </main>
</Layout>
